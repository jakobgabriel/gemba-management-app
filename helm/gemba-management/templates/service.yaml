apiVersion: v1
kind: Service
metadata:
  name: {{ include "gemba-management.fullname" . }}
  labels:
    {{- include "gemba-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gemba-management.selectorLabels" . | nindent 4 }}
